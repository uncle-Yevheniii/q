import { Box } from "@mui/material";

export default function AboutLayout() {
  return (
    <Box
      component="section"
      sx={{ p: 2, border: "1px dashed grey", display: "flex", height: "100vh" }}
    >
      AboutLayout
    </Box>
  );
}
